{
  "name": "@manael/website",
  "private": true,
  "scripts": {
    "build": "hugo --minify",
    "dev": "hugo --watch",
    "generate-types": "wrangler types",
    "type-check": "pnpm run generate-types && tsc --noEmit"
  },
  "devDependencies": {
    "autoprefixer": "10.5.4",
    "postcss": "8.5.19",
    "postcss-cli": "11.0.1",
    "wrangler": "4.112.0"
  }
}
